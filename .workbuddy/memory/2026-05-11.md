# 2026-05-11 工作日志

## 侧边栏三分组重构
- 将左侧菜单从平铺列表重构为三个配置分组：
  - **算力服务器**（蓝色左边框 #3b82f6）：数量、GPU规格、存储网卡
  - **存储服务器**（翠绿左边框 #10b981）：数量、网卡型号、网卡数量
  - **组网架构**（琥珀左边框 #fbbf24）：架构选择
- 精简选项名称：`算力服务器数量`→`数量`、`GPU规格 / 网卡配置`→`GPU 规格`、`算力服务器存储网卡配置`→`存储网卡`、`存储服务器数量`→`数量`、`存储网卡配置`→`网卡型号`、`存储网卡数量`→`网卡数量`、`组网架构`→`架构`
- 精简下拉选项文本：去掉"NVIDIA"前缀，B300选项从"NVIDIA B300 (8-Rail, 8x 800G)"精简为"B300 8-Rail"
- 每个分组带彩色左边框 + 图标 header，视觉层次清晰
- CSS 新增 .config-group / .config-group-header / .config-group-body / .config-item / .config-label / .config-hint / .config-select / .config-input 等样式类
- 所有表单元素 ID 保持不变，JS 事件绑定无需修改

## 修复64台服务器POD划分
- **问题**：64台服务器被错误分成2个POD（每POD 32台）
- **修复**：hardware-engine.js POD划分逻辑调整：
  - `serverCount <= 64` → 1个POD（原来32台以下才1个POD）
  - `serverCount <= 128` → 2个POD（原逻辑不变）
  - 其余逻辑不变
- 修复后64台 B300 计算结果：podCount=1, serversPerPod=64, leaf=16, spine=8
