# 2026-05-12 工作日志

## 修改默认值与导出逻辑

### 1. 启动默认值调整
- **算力服务器数量**：128 台（已为此值，无需修改）
- **GPU 规格**：B300_8（已为此值，无需修改）
- **组网架构**：virtual-dual-plane（已为此值，无需修改）
- **连线明暗**：0.15 → 0.60（60%）
- **连线粗细**：1.5 → 2.0

### 修改文件
| 文件 | 修改内容 |
|------|----------|
| `js/config.js` | `LINK_STYLE.opacity.default = 0.60`，`strokeWidth.default = 2.0` |
| `js/state.js` | `linkStyle.opacity = 0.60`，`strokeWidth = 2.0` |
| `js/storage.js` | `getDefaults()` 中 `linkStyle` 同步更新 |
| `index.html` | range input value 和显示文本同步更新 |

### 2. 导出时按当前设置值导出
- **问题**：`js/export.js` 中 `EXPORT_CSS` 硬编码 `opacity: 0.15`，导出时忽略用户当前调整值
- **解决**：改为 `getExportCSS()` 动态函数，从 `appState.linkStyle` 实时读取 `opacity` 和 `strokeWidth`，注入导出 SVG 的 `<style>` 标签中
- **修改文件**：`js/export.js`（引入 `appState`，替换常量 CSS 为动态函数）

## 修改GPU规格选项并新增算力网卡选项

### 需求
1. GPU规格下拉选项调整为：8×B300 SXM6 / 8×B200 SXM6 / 8×H200 SXM5
2. 在GPU规格下新增"算力网卡"选项：8×CX8 800G / 8×CX8 2×400G / 8×CX7 400G

### 修改文件
| 文件 | 修改内容 |
|------|----------|
| `js/config.js` | 更新 `GPU_SPECS` 键名和 label；新增 `COMPUTE_NIC_SPECS` 配置表 |
| `index.html` | 更新 GPU 规格 `<select>` 选项；新增算力网卡 `<select>` |
| `js/state.js` | 新增 `computeNic: 'CX8_800G'` 状态字段；更新 `gpuType` 默认值为 `B300_SXM6` |
| `js/storage.js` | 持久化新增 `computeNic`；更新 gpuType 校验列表；reset 时同步 UI |
| `js/app.js` | 读取 `computeNic` 并传入 options；绑定 change 事件；syncUIFromState 同步 |
| `js/layout-engine.js` | 导入 `GPU_SPECS`/`COMPUTE_NIC_SPECS`；railCount 改为从配置读取；tooltip 动态显示算力网卡型号 |
| `js/hardware-engine.js` | 更新默认 gpuType 为 `B300_SXM6`；fallback 同步更新 |
| `js/stats-panel.js` | 导入 `COMPUTE_NIC_SPECS`；summary 中显示算力网卡信息 |

## 优化省略标注显示样式

### 需求
1. 省略标注文字"← X →"字号放大、颜色更明显（金色 #fbbf24）
2. X 改为显示**实际总设备数量**（非省略数量）
3. POD 省略标注垂直居中于 POD 框内

### 修改文件
| 文件 | 修改内容 |
|------|----------|
| `js/layout-engine.js` | POD gap: podLabel 显示总 POD 数、字号 18、金色、垂直居中；podSubLabel 显示"共 N 个POD"。Spine gap 新增 `totalCount`。Leaf gap 新增 `totalCount`。Server ellipsis `count` 改为 `actualServersInPod`。 |
| `js/svg-renderer.js` | Spine/Leaf/Server 省略标注：字号 15、颜色 #fbbf24；Spine/Leaf 显示 `totalCount`；Server 显示 `count`（已改为总数）；Spine 副标注字号 11、颜色 #94a3b8。 |

### 标注数值对照
| 层级 | 显示内容 | 数据来源 |
|------|----------|----------|
| 算力服务器 | `← 64 →` | 每个 POD 实际服务器总数 |
| Leaf 交换机 | `← 8 →` | 每 POD Leaf 对总数 |
| Spine 交换机 | `← 4 →` | 全局 Spine 总数 |
| POD | `← 4 →` | 全局 POD 总数 |
